
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; background: #f5f5f5; }
    h1 { color: #333; }
    h2 { color: #21da7e; }    
    table { width: 100%; border-collapse: collapse; background: white; }
    th, td { border: 1px solid #ddd; padding: 8px 12px; text-align: left; }
    th { background: #eee; }
    .status-badge {
      padding: 4px 8px;
      border-radius: 4px;
      color: white;
      font-size: 0.85rem;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <h1><%= title %></h1>
  <% if (data_yes) { %>
  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Заказ-наряд</th>
        <th>Статус</th> 
        <th>Дата заявки</th>                
      </tr>
    </thead>
    <tbody>
      <% rows.forEach(rows => { %>
        <tr>
          <td><%= rows.ID %></td>
          <td><%= rows.NUMBER %></td>
          <td><%= rows.STATUS %></td> 
          <td><%= rows.formattedDate %></td>           

          <td>  
            <form action="/view/<%= rows.ID%>" method="get">
              <button type="submit">Открыть</button>
            </form></td>                    
        </tr>
      <% }); %>
    </tbody>
  </table>
  <% } else { %>
  <h2>Записей не найдено...</h2>
  <% } %>
  <form action="/add" method="get">
    <button type="submit">Добавить</button>
  </form>
  <form action="/update" method="get">
    <button type="submit">Обновить</button>
  </form>
</body>
</html>